int main(int argc, const char * argv[]) {
    int bin = 0, k = 1;

    while (num)
    {
        bin += (num % 2) * k;
        k *= 10;
        num /= 2;
    }

    std::cout << bin << std::endl;


    
    return 0;
}

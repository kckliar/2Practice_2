int main(int argc, const char * argv[]) {
    
    zer(char *s) {
    short i;
    long res = 0;
   
    for ( i = 0 ; i < sizeof (long) * 8 && s[i] ; ++i ){
       res <<= 1;
       res |= (s[i] - '0');
    }
    

    std::cout << res << std::endl;
}

    return 0;
}

